{
  "name": "tester-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Tester Smart Watch Pro — landing page with Express contact-form backend writing to Google Sheets.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "googleapis": "^172.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "supabase": "^2.101.0"
  }
}
